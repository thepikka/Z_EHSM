<mvc:View controllerName="ehsm.controller.Risks"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.f">
    <Page title="Risk Assessment" showNavButton="true" navButtonPress=".onNavBack">
        <headerContent>
            <core:Fragment fragmentName="ehsm.view.Header" type="XML" />
        </headerContent>
        <content>
            <Table id="risksTable" items="{path: 'riskModel>/results'}" growing="true" growingThreshold="20" class="sapUiSmallMargin">
                <headerToolbar>
                    <OverflowToolbar>
                        <Title text="Risks" level="H2"/>
                    </OverflowToolbar>
                </headerToolbar>
                <columns>
                    <Column><Text text="Risk ID"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Description"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Category"/></Column>
                    <Column><Text text="Severity"/></Column>
                    <Column><Text text="Likelihood"/></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Mitigation"/></Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{riskModel>RiskId}"/>
                            <Text text="{riskModel>RiskDescription}"/>
                            <Text text="{riskModel>RiskCategory}"/>
                            <ObjectStatus text="{riskModel>RiskSeverity}" state="{= ${riskModel>RiskSeverity} === 'High' ? 'Error' : (${riskModel>RiskSeverity} === 'Medium' ? 'Warning' : 'Success')}"/>
                            <Text text="{riskModel>Likelihood}"/>
                            <Text text="{riskModel>MitigationMeasures}"/>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>
