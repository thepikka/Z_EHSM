<mvc:View controllerName="ehsm.controller.App"
    displayBlock="true"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">
    <Shell>
        <App id="app">
            <!-- Global header could be added here if needed, or inside each page. 
                 Since the user wants a global logout/profile, ShellBar is better. -->
        </App>
    </Shell>
</mvc:View>