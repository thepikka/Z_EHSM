<mvc:View controllerName="ehsm.controller.Incidents"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.f">
    <Page title="Incident Management" showNavButton="true" navButtonPress=".onNavBack">
        <headerContent>
            <core:Fragment fragmentName="ehsm.view.Header" type="XML" />
        </headerContent>
        <content>
            <Table id="incidentsTable" items="{path: 'incidentModel>/results'}" growing="true" growingThreshold="20" class="sapUiSmallMargin">
                <headerToolbar>
                    <OverflowToolbar>
                        <Title text="Incidents" level="H2"/>
                    </OverflowToolbar>
                </headerToolbar>
                <columns>
                    <Column><Text text="Incident ID"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Description"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Category"/></Column>
                    <Column><Text text="Priority"/></Column>
                    <Column><Text text="Status"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Date"/></Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{IncidentId}"/>
                            <Text text="{IncidentDescription}"/>
                            <Text text="{IncidentCategory}"/>
                            <ObjectStatus text="{IncidentPriority}" state="{= ${IncidentPriority} === 'High' ? 'Error' : (${IncidentPriority} === 'Medium' ? 'Warning' : 'Success')}"/>
                            <ObjectStatus text="{IncidentStatus}" state="{= ${IncidentStatus} === 'Open' ? 'Error' : 'Success'}"/>
                            <Text text="{path: 'IncidentDate', type: 'sap.ui.model.type.Date', formatOptions: { pattern: 'dd/MM/yyyy' }}"/>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>
